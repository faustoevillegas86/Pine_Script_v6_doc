# Core dependencies
crawl4ai>=0.4.0
beautifulsoup4~=4.12
requests~=2.26
playwright>=1.49.0
aiofiles>=24.1.0
asyncio>=3.4.3

# Processing dependencies
markdown>=3.4
python-dotenv~=1.0
colorama~=0.4

# Optional dependencies for enhanced functionality
tqdm~=4.65.0  # Progress bars
pydantic>=2.10  # Data validation

# Note: typing, datetime, and re are part of Python's standard library
# and do not need to be installed via pip